package ru.sapa.gadalka_backend.api.dto.ai;

/**
 * Настройка «токенов мыслей» (reasoning) для reasoning-моделей.
 *
 * <p>Модели вроде deepseek-v4-flash перед ответом рассуждают «про себя». Эти рассуждения
 * считаются выходными токенами: они расходуют бюджет max_tokens и оплачиваются по
 * общему тарифу, хотя пользователю не показываются. На практике это приводило к тому,
 * что интерпретация одной карты сжигала весь лимит в 550 токенов и возвращала пустой
 * текст (finish_reason=length), после чего шёл повторный запрос с лимитом 2200.
 *
 * <p>Допустимые значения effort: max, xhigh, high, medium, low, minimal, none.
 * Значение "none" полностью отключает рассуждения — подходит для творческой генерации,
 * где пошаговый вывод не нужен. Модели, у которых рассуждения обязательны, "none" отклоняют.
 *
 * <p>Документация: https://docs.aitunnel.ru/api/reasoning
 */
public record AiReasoning(String effort) {
}
