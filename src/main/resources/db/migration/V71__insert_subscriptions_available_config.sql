-- Тоггл доступности подписок для обычных пользователей. Пока фича в закрытом
-- тесте: по умолчанию 'false' — подписки видят и могут купить только админы
-- (как и было захардкожено раньше). Включить всем — правкой этой записи из
-- админ-панели, без деплоя.

INSERT INTO system_config (config_key, config_value, description, created_at, updated_at)
VALUES
    ('SUBSCRIPTIONS_AVAILABLE_FOR_ALL_USERS',
     'false',
     'Доступность покупки подписок для всех пользователей (false = видят только админы)',
     NOW(),
     NOW());
